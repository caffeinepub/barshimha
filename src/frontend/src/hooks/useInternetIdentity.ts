// Re-export from the core infrastructure package so local imports resolve correctly
export {
  useInternetIdentity,
  InternetIdentityProvider,
} from "@caffeineai/core-infrastructure";
export type {
  InternetIdentityContext,
  Status,
} from "@caffeineai/core-infrastructure";
